# 面试宝典文章格式规范

1. **问题使用二级标题** - `## 问题内容？` 格式
2. **⭐标记面试真题** - 标题前加 `⭐`表示面试中遇到的真实题目，只能由我手动添加表示真题，你不可使用
3. **问题要像真实面试口吻** - 避免突兀的提问，比如用"JS是单线程的，如何实现异步？"而非"事件循环机制是怎样的？"
4. **答案记忆卡片式（简洁优先）** - 答案结构化、易背诵，不用大段口语长句。**每题结构：记忆锚点（一行口诀/链路）→ 编号要点（第一级骨架）→ 必要时二级缩进展开细节**。不要用"回答："这两个字开头，不要写"这个问题面试很喜欢问"之类的铺垫话术。**每题答案控制在 15 行以内，第一级要点不超过 6 条。**
5. **两级树状结构，禁止题目内嵌套标题** - 第一级用编号列表（`1.` `2.`），每项 = 加粗关键词 + 主干短句（≤30 字）；细节、术语解释、例子用第二级缩进列表（3 空格 `-`）。**题目内部（答案中）禁止出现任何标题**（`###`、`####` 等）。对比场景可用表格（列名如"组件/角色/作用"）；代码非必要不用，只留核心片段（≤5 行）。
6. **少用表格** - 仅在必要时使用表格对比
7. **代码精简** - 非必要不使用代码，只保留核心代码，避免冗长示例
8. **每文件一主题** - 如数据类型、函数、面向对象等

## 文章 Frontmatter 规范

```yaml
---
title: 文章标题
date: 2024-01-01
categories: [分类]
order: 0  # 可选，越小越靠前
---
```

## HoverComment 组件使用规范

**使用时机**：默认**不使用**，仅当用户明确要求"使用 hovercomment 解释/加悬浮注释"时才添加。

**使用格式**：

```markdown
<HoverComment text="术语名称" comment="详细解释内容（支持Markdown）" />
```

**示例**：

```markdown
<HoverComment text="直接内存" comment="用`ByteBuffer.allocateDirect()`分配的堆外内存，不走JVM堆，直接在操作系统内存申请。访问快（零拷贝），但分配回收成本高。" />
```

**注意事项**：

- 多个组件在同一行时，用顿号（、）分隔
- comment 内容支持 Markdown 语法（代码、链接等）
- 组件已优化为行内显示，不会换行

## 关键规则

- `/blogs/` 目录下文章自动归类到博客，侧边栏按 `order` + `date` 排序
- `frontmatter.article = false` 的文章不显示在博客列表
- 私有目录 (`/private/`, `/trash/`) 仅开发环境可见
- 全站使用中文

---

## 面试题与答题历史

### 出题优先基于简历

- 出题前先读项目根 `AGENTS.md` 获取**简历文件路径**，再读简历（`src/private/resume/黄锦锋-Java后端-AI应用-27届.html`）提取技能栈、项目、实习经历，**优先基于简历出题**——题目从简历出发，简历没写的不假设为候选人经历
- 项目相关题以简历中的项目描述为回答素材（如 FlowMind 的 AI 流程设计、OISG 的 Dify + Spring AI 工具接入）；简历内容不复制进仓库，更新简历后无需改 AGENTS.md

### 题目融入到现有技术文档，不独立建题库文件

- ❌ 不允许单独建 `redis-题库.md`、`spring-题库.md`、`agent-interview-qa.md` 这种题库文件
- ✅ 应当把题目融入到对应分类下已有的技术文档（如 `数据库/redis.md`、`框架/spring.md`、`Agent 开发/基本知识.md`）
- ✅ 现有文档已有这道题时（如 `数据库/redis.md` 已存在 Redis 缓存穿透/雪崩/击穿），只需在题目下方加一行 `→ [回答历史](...)` 链接即可，不要重复题目正文
- 只在没有对应技术分类(如软素质/沟通类)时才放到最贴近的现有技术文档里(`基础知识/协作交流.md`、`开发工具/claudecode.md`、`我的项目/业务实现.md` 等)
- 极少数题目无任何归属时才考虑新建独立分类

### 标题命名

- 题目标题**不用序号**（不用 `Q1.` `1.` `1）`），纯文字描述
- 标题在题库（融入的文档）和答题记录中**完全一致**（一字不差）
- 标题里避免出现 `` ` `` `#` `|` 等特殊字符，影响锚点稳定性

### 题目示例：融入到现有 redis.md

```markdown
## Redis 缓存穿透、缓存雪崩和缓存击穿分别是什么？如何解决？

→ [回答历史](/private/series/答题历史/数据库/redis-答题记录.md#redis-缓存穿透击穿雪崩区别)

... 已有的标准答案 ...
```

---

## 答题历史目录结构

`src/private/series/答题历史/` 与 `src/series/knowledge/` 顶层分类同构,每个分类下放 `{topic}-答题记录.md`。**答题历史目录独立于 `knowledge/` 之外**(位于 `src/private/series/答题历史/`),这样既方便统一隐藏侧边栏,也便于跨分类跳转。

```
src/
├── private/
│   └── series/
│       └── 答题历史/           ← 答题记录(独立目录,不与 knowledge 同级)
│           ├── Java/
│           │   └── java-答题记录.md
│           ├── 框架/
│           │   ├── spring-答题记录.md
│           │   ├── springcloud-答题记录.md
│           │   └── mybatis-答题记录.md
│           ├── 数据库/
│           │   ├── redis-答题记录.md
│           │   └── mysql-答题记录.md
│           ├── 我的项目/
│           │   ├── gf-cms-答题记录.md
│           │   └── 业务-答题记录.md
│           ├── Agent 开发/
│           │   └── 基本知识-答题记录.md
│           └── 基础知识/
│               └── 非技术面试问答-答题记录.md
```

### 侧边栏隐藏

`src/private/series/答题历史/` 整个目录**不在面试宝典侧边栏展示**。它仅作为内部跳转目标。通过显式配置 `"structure"` 模式 + 把答题历史目录排除在 scan 范围之外实现。在 [`src/.vuepress/sidebar/zh.ts`](https://github.com/Moonlight168/vuepress-theme-hope/blob/main/my-docs/src/.vuepress/sidebar/zh.ts) 中：

```ts
// /series/knowledge/ 用 structure 自动扫描（扫描到所有 knowledge 子目录,但跳过答题历史）
"/series/knowledge/": "structure",
"/series/myprojects/": "structure",
// 答题历史路径显式置空数组,屏蔽其在任何父级 sidebar 中的呈现
"/private/series/答题历史/": [],
```

---

## 题库 ↔ 答题历史的跳转链接

### 路径常量

路径模板集中维护在项目根目录的 [`.env`](https://github.com/Moonlight168/vuepress-theme-hope/blob/main/my-docs/.env) 与 [`src/.vuepress/config/constants.ts`](https://github.com/Moonlight168/vuepress-theme-hope/blob/main/my-docs/src/.vuepress/config/constants.ts)：

```bash
# .env
ANSWER_HISTORY_REL=/private/series/答题历史
```

```ts
// constants.ts 提供 answerHistoryRel(fromFile) 计算相对路径
```

> **实际改动**：当答题历史目录搬迁时,只需更新 `.env` 的 `ANSWER_HISTORY_REL` 与 `constants.ts` 中的目标目录,然后用脚本替换所有文件中的 `/private/series/答题历史/` 字面量即可。

### 路径规范（最常出错的地方）

**目录布局**:

```
src/
├── series/
│   ├── knowledge/                   ← 题库融合到的技术文档
│   │   └── {分类}/{topic}.md
│   └── 答题历史/                    ← 答题记录（独立目录、不与 knowledge 同级）
│       └── {分类}/{topic}-答题记录.md
```

答题历史跳转链接直接写**站点根绝对路径** `/private/series/答题历史/{分类}/{topic}-答题记录.md#锚点`。

**绝对路径常量**集中维护在项目根目录的 [`.env`](https://github.com/Moonlight168/vuepress-theme-hope/blob/main/my-docs/.env)：

```bash
# .env
ANSWER_HISTORY_REL=/private/series/答题历史
```

> **实际改动**：当答题历史目录搬迁时,只需更新 `.env` 的 `ANSWER_HISTORY_REL`,然后用脚本替换所有文件中的 `/private/series/答题历史/` 字面量即可。
> `src/.vuepress/config/constants.ts` 不再提供路径常量(钩子直接在 config.ts 里读 .env)。
>
> **⚠️ 失败经验总结**(避免重蹈):
> 1. **VuePress 2 不暴露 `markdown.link.preprocess` 钩子**(已查 `@vuepress/markdown` 源码,`MarkdownLink` 接口只有 `raw / relative / absolute`),钩子静默不生效,渲染出相对路径 → 404。
> 2. **尝试过 `extendsMarkdown` + markdown-it `core.ruler` 改 link token**(在 markdown-it 输出 HTML 前注入前缀),实际 VuePress 仍渲染出相对路径 —— 可能 VuePress 自己有二次处理链接的 plugin,优先级更高。
> **结论：绝对路径字面量是 VuePress 2 下唯一可靠的方案。**

- `knowledge/Java/集合.md` → `/private/series/答题历史/Java/java-答题记录.md`
- `knowledge/框架/spring.md` → `/private/series/答题历史/框架/spring-答题记录.md`
- `knowledge/Agent 开发/基本知识.md` → `/private/series/答题历史/Agent%20开发/基本知识-答题记录.md`（分类名含空格，路径写 `%20`）
- `knowledge/基础知识/协作交流.md` → `/private/series/答题历史/基础知识/非技术面试问答-答题记录.md`（软素质题融入此处，文件名保留旧名以追溯原始来源）

**不要写成**:

- `../../../private/series/答题历史/...`（相对路径,跨多文件位置时容易算错层数）
- `../../答题历史/...`（少一层,无法跳转）
- `../答题历史/...`（更少一层）
- `分类/xxx-答题记录.md`（短键名,会被 VuePress 当相对路径处理成 404）
- **`/private/series/答题历史/Agent 开发/...`（路径含字面空格）** —— ⚠️ VuePress 无法把含空格的 Markdown 链接解析成 `<a>` 链接，会渲染成纯文本，点不了。**分类名含空格时（如 `Agent 开发`），路径里的空格必须写成 URL 编码 `%20`**：
  - ✅ `/private/series/答题历史/Agent%20开发/前沿技术-答题记录.md#锚点`
  - ❌ `/private/series/答题历史/Agent 开发/前沿技术-答题记录.md#锚点`

任何路径写错,链接就只是纯文本不可点击。

### 链接语法

```markdown
→ [回答历史](/private/series/答题历史/{分类}/{topic}-答题记录.md#锚点)
```

> **含空格分类名**：分类目录名含空格时（如 `Agent 开发`），路径中的空格写成 `%20`：
> ```markdown
> → [回答历史](/private/series/答题历史/Agent%20开发/前沿技术-答题记录.md#锚点)
> ```

### 锚点规则

- 中文标题的锚点：Markdown 会自动把中文转为 URL 编码，多数渲染器认直接写中文
- 保险做法：标题里去掉 `` ` `` `#` `|` 等特殊字符
- 示例：标题 `Redis 缓存穿透、击穿、雪崩区别？` → 锚点 `#redis-缓存穿透击穿雪崩区别`（小写 + 去符号）

---

## 答题记录文件格式

```markdown
# {Topic} 面试答题记录

---

## 问题标题（和题库完全一致）

- **YYYY-MM-DD**：被面试人的原始回答（原话记录）
- **YYYY-MM-DD**：第二次回答（如有进步则对比明显）
```

**规则**：

1. 标题和题库**一模一样**
2. 每次回答前面加日期，格式 `**YYYY-MM-DD**`
3. 记录**原话**，不加工、不美化
4. 同一天多次回答只保留最后版本，或追加写

---

## 添加新问题的流程

1. 面试官提问 → 被面试人回答
2. 把问题 + 标准答案**融入到现有知识分类的文档**（如 `Java/集合.md`、`框架/spring.md`、`Agent 开发/基本知识.md`），只在该题旁边紧跟一行 `→ [回答历史](...)` 链接
3. 把被面试人的原话写入 `答题历史/{分类}/{topic}-答题记录.md`，日期标注
4. 如果题目**没有**对应技术分类（例如软素质/沟通类），融入到最贴近的现有技术文档（例如软素质类题目融入 `基础知识/协作交流.md`，AI 协作类融入 `开发工具/claudecode.md`）
5. 极个别题目没归属才考虑新建独立分类——但**优先融入现有文档**

---

## 面试官行为规范

- **面试官站在真实面试官角度出题**，不是站在 tutor 角度
- 被面试人回答后，面试官给出**简洁点评**（对的确认、错的纠正、漏的补充），然后直接进入下一题
- 不展开长篇讲解，除非被面试人追问
- 回答中暴露的薄弱点，可以追加追问深挖
- 语气像真实的面试官——专业、简洁、不咄咄逼人也不过度鼓励

---

## 标题命名（在整个面试宝典范畴）

- 文件 frontmatter 的 `title` 不带"面试题"等后缀（如 `title: LangGraph` 而非 `title: LangGraph 面试题`）
- 侧边栏中文本由 `dir.text` 决定,可以包含附加信息

---

## 复用检查清单

新增或修改面试题时逐项核对：

- [ ] 题目已融入现有技术文档,没有新建独立题库文件（除非无对应分类）
- [ ] 题目无序号编号（没有 `Q1.` `1.` 等）
- [ ] 标题在原文档和答题记录文件中**完全一致**
- [ ] 题库（融入到原文档中的题）**只在答题记录里有实际回答时**才紧跟一行 `→ [回答历史](...)` 链接；无回答不挂
- [ ] 答案无嵌套标题（`###`/`####`），分点用编号列表 + 加粗关键词，细节用二级缩进列表
- [ ] 每题答案：记忆锚点（一行） + 编号要点（≤6 条）+ 必要时二级展开，≤15 行
- [ ] 追问仅在真正高频时加，格式 `**追问**：xxx？→ 一句话答案`；HoverComment 仅用户点名时用
- [ ] 链接是有效的绝对路径 `/private/series/答题历史/...`,绝不是缺层或相对路径
- [ ] 分类名含空格时（如 `Agent 开发`）链接里写成 `%20`（`/private/series/答题历史/Agent%20开发/...`）,否则会被当纯文本
- [ ] 答题记录有日期前缀 `**YYYY-MM-DD**`
- [ ] 答题记录文件位置在 `src/private/series/答题历史/{分类}/` 下,文件名 `*-答题记录.md`
- [ ] 路径前缀用 `/private/series/答题历史/`（站点根出发）,对应 `.env` 中的 `ANSWER_HISTORY_REL`
- [ ] topic 粒度合理（不要全塞一起，也不要每道题一个文件）
- [ ] 文件 frontmatter `title` 不带"面试题"等冗余后缀
- [ ] `src/.vuepress/sidebar/zh.ts` 中走精确 prefix 列表,不要用 `structure`（避免扫到"答题历史"）
