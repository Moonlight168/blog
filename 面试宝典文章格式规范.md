# 面试宝典文章格式规范

1. **问题使用二级标题** - `## 问题内容？` 格式
2. **⭐标记面试真题** - 标题前加 `⭐`表示面试中遇到的真实题目，只能由我手动添加表示真题，你不可使用
3. **问题要像真实面试口吻** - 避免突兀的提问，比如用"JS是单线程的，如何实现异步？"而非"事件循环机制是怎样的？"
4. **回答口语化问答** - 直接简洁回答，不冗长解释，不要用"回答："这两个字开头
5. **优先使用编号列表** - 使用 `1.` `2.` 而非多级 `###` 标题
6. **少用表格** - 仅在必要时使用表格对比
7. **代码精简** - 非必要不使用代码，只保留核心代码，避免冗长示例
8. **每文件一主题** - 如数据类型、函数、面向对象等

## 文章 Frontmatter 规范

```yaml
---
title: 文章标题
date: 2024-01-01
categories: [分类]
order: 0  # 可选，越小越靠前
---
```

## HoverComment 组件使用规范

当用户要求"使用 hovercomment 加以解释"时，为特定术语添加悬浮说明组件。

**使用格式**：

```markdown
<HoverComment text="术语名称" comment="详细解释内容（支持Markdown）" />
```

**示例**：

```markdown
<HoverComment text="直接内存" comment="用`ByteBuffer.allocateDirect()`分配的堆外内存，不走JVM堆，直接在操作系统内存申请。访问快（零拷贝），但分配回收成本高。" />
```

**注意事项**：

- 多个组件在同一行时，用顿号（、）分隔
- comment 内容支持 Markdown 语法（代码、链接等）
- 组件已优化为行内显示，不会换行

## 关键规则

- `/blogs/` 目录下文章自动归类到博客，侧边栏按 `order` + `date` 排序
- `frontmatter.article = false` 的文章不显示在博客列表
- 私有目录 (`/private/`, `/trash/`) 仅开发环境可见
- 全站使用中文
