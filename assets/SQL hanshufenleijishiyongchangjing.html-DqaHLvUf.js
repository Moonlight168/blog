import{_ as n,c as a,a as e,o as t}from"./app-CFgUeV5-.js";const o={};function l(c,s){return t(),a("div",null,s[0]||(s[0]=[e(`<h3 id="sql-函数分类及使用场景" tabindex="-1"><a class="header-anchor" href="#sql-函数分类及使用场景"><span><strong>SQL 函数分类及使用场景</strong></span></a></h3><h4 id="一、单行函数-scalar-functions" tabindex="-1"><a class="header-anchor" href="#一、单行函数-scalar-functions"><span><strong>一、单行函数（Scalar Functions）</strong></span></a></h4><p><strong>定义</strong>：对每一行数据独立处理，每行输入返回一个结果，与其他行无关。<br><strong>特点</strong>：输入一行，输出一行，不依赖其他行的数据。<br><strong>常见分类及示例</strong>：</p><h5 id="_1-字符串函数-string-functions" tabindex="-1"><a class="header-anchor" href="#_1-字符串函数-string-functions"><span><strong>1. 字符串函数（String Functions）</strong></span></a></h5><ul><li><strong>作用</strong>：处理字符串数据（拼接、截取、转换大小写、计算长度等）。 <ul><li><code>CONCAT(str1, str2)</code>：拼接字符串（MySQL、SQL Server）。<div class="language-sql line-numbers-mode" data-highlighter="prismjs" data-ext="sql" data-title="sql"><pre><code><span class="line"><span class="token keyword">SELECT</span> CONCAT<span class="token punctuation">(</span>first_name<span class="token punctuation">,</span> <span class="token string">&#39; &#39;</span><span class="token punctuation">,</span> last_name<span class="token punctuation">)</span> <span class="token keyword">AS</span> full_name <span class="token keyword">FROM</span> users<span class="token punctuation">;</span></span>
<span class="line"></span></code></pre><div class="line-numbers" aria-hidden="true" style="counter-reset:line-number 0;"><div class="line-number"></div></div></div></li><li><code>SUBSTRING(str, start, length)</code>：截取子字符串（MySQL）。<div class="language-sql line-numbers-mode" data-highlighter="prismjs" data-ext="sql" data-title="sql"><pre><code><span class="line"><span class="token keyword">SELECT</span> SUBSTRING<span class="token punctuation">(</span>email<span class="token punctuation">,</span> <span class="token number">1</span><span class="token punctuation">,</span> <span class="token number">5</span><span class="token punctuation">)</span> <span class="token keyword">AS</span> short_email <span class="token keyword">FROM</span> users<span class="token punctuation">;</span></span>
<span class="line"></span></code></pre><div class="line-numbers" aria-hidden="true" style="counter-reset:line-number 0;"><div class="line-number"></div></div></div></li><li><code>UPPER(str)</code> / <code>LOWER(str)</code>：转换大小写。</li><li><code>LENGTH(str)</code> / <code>CHAR_LENGTH(str)</code>：计算字节长度/字符数（MySQL）。</li><li><code>TRIM()</code> / <code>LTRIM()</code> / <code>RTRIM()</code>：去除首尾/左/右空格。</li></ul></li></ul><h5 id="_2-数值函数-numeric-functions" tabindex="-1"><a class="header-anchor" href="#_2-数值函数-numeric-functions"><span><strong>2. 数值函数（Numeric Functions）</strong></span></a></h5><ul><li><strong>作用</strong>：处理数值数据（数学运算、取整、随机数等）。 <ul><li><code>ABS(num)</code>：绝对值。</li><li><code>ROUND(num, decimals)</code>：四舍五入。<div class="language-sql line-numbers-mode" data-highlighter="prismjs" data-ext="sql" data-title="sql"><pre><code><span class="line"><span class="token keyword">SELECT</span> <span class="token function">ROUND</span><span class="token punctuation">(</span>price<span class="token punctuation">,</span> <span class="token number">2</span><span class="token punctuation">)</span> <span class="token keyword">AS</span> rounded_price <span class="token keyword">FROM</span> products<span class="token punctuation">;</span></span>
<span class="line"></span></code></pre><div class="line-numbers" aria-hidden="true" style="counter-reset:line-number 0;"><div class="line-number"></div></div></div></li><li><code>FLOOR(num)</code> / <code>CEILING(num)</code>：向下取整/向上取整。</li><li><code>RAND()</code>：生成随机数（MySQL）。</li></ul></li></ul><h5 id="_3-日期和时间函数-date-time-functions" tabindex="-1"><a class="header-anchor" href="#_3-日期和时间函数-date-time-functions"><span><strong>3. 日期和时间函数（Date/Time Functions）</strong></span></a></h5><ul><li><strong>作用</strong>：处理日期和时间数据（提取部分字段、计算间隔、格式化等）。 <ul><li><code>CURDATE()</code> / <code>CURTIME()</code>：获取当前日期/时间（MySQL）。</li><li><code>DATE_FORMAT(date, format)</code>：格式化日期。<div class="language-sql line-numbers-mode" data-highlighter="prismjs" data-ext="sql" data-title="sql"><pre><code><span class="line"><span class="token keyword">SELECT</span> DATE_FORMAT<span class="token punctuation">(</span>order_date<span class="token punctuation">,</span> <span class="token string">&#39;%Y-%m-%d&#39;</span><span class="token punctuation">)</span> <span class="token keyword">AS</span> formatted_date <span class="token keyword">FROM</span> orders<span class="token punctuation">;</span></span>
<span class="line"></span></code></pre><div class="line-numbers" aria-hidden="true" style="counter-reset:line-number 0;"><div class="line-number"></div></div></div></li><li><code>DATEDIFF(date1, date2)</code>：计算两个日期的天数差。<div class="language-sql line-numbers-mode" data-highlighter="prismjs" data-ext="sql" data-title="sql"><pre><code><span class="line"><span class="token keyword">SELECT</span> DATEDIFF<span class="token punctuation">(</span>return_date<span class="token punctuation">,</span> order_date<span class="token punctuation">)</span> <span class="token keyword">AS</span> days_diff <span class="token keyword">FROM</span> orders<span class="token punctuation">;</span></span>
<span class="line"></span></code></pre><div class="line-numbers" aria-hidden="true" style="counter-reset:line-number 0;"><div class="line-number"></div></div></div></li></ul></li></ul><h5 id="_4-类型转换函数-conversion-functions" tabindex="-1"><a class="header-anchor" href="#_4-类型转换函数-conversion-functions"><span><strong>4. 类型转换函数（Conversion Functions）</strong></span></a></h5><ul><li><strong>作用</strong>：转换数据类型（如字符串转数值、日期转字符串等）。 <ul><li><code>CAST(expr AS type)</code>：显式类型转换。<div class="language-sql line-numbers-mode" data-highlighter="prismjs" data-ext="sql" data-title="sql"><pre><code><span class="line"><span class="token keyword">SELECT</span> CAST<span class="token punctuation">(</span><span class="token string">&#39;123&#39;</span> <span class="token keyword">AS</span> SIGNED<span class="token punctuation">)</span> <span class="token keyword">AS</span> num <span class="token keyword">FROM</span> dual<span class="token punctuation">;</span> <span class="token comment">-- MySQL 中转为整数</span></span>
<span class="line"></span></code></pre><div class="line-numbers" aria-hidden="true" style="counter-reset:line-number 0;"><div class="line-number"></div></div></div></li><li><code>CONVERT(expr, type)</code>：另一种转换方式（不同数据库语法可能不同）。</li></ul></li></ul><h5 id="_5-条件函数-conditional-functions" tabindex="-1"><a class="header-anchor" href="#_5-条件函数-conditional-functions"><span><strong>5. 条件函数（Conditional Functions）</strong></span></a></h5><ul><li><strong>作用</strong>：根据条件返回不同结果。 <ul><li><code>IF(condition, value_if_true, value_if_false)</code>：简单条件判断（MySQL）。<div class="language-sql line-numbers-mode" data-highlighter="prismjs" data-ext="sql" data-title="sql"><pre><code><span class="line"><span class="token keyword">SELECT</span> price<span class="token punctuation">,</span> <span class="token keyword">IF</span><span class="token punctuation">(</span>price <span class="token operator">&gt;</span> <span class="token number">100</span><span class="token punctuation">,</span> <span class="token string">&#39;高价&#39;</span><span class="token punctuation">,</span> <span class="token string">&#39;低价&#39;</span><span class="token punctuation">)</span> <span class="token keyword">AS</span> price_level <span class="token keyword">FROM</span> products<span class="token punctuation">;</span></span>
<span class="line"></span></code></pre><div class="line-numbers" aria-hidden="true" style="counter-reset:line-number 0;"><div class="line-number"></div></div></div></li><li><code>CASE WHEN THEN ELSE END</code>：多条件判断。<div class="language-sql line-numbers-mode" data-highlighter="prismjs" data-ext="sql" data-title="sql"><pre><code><span class="line"><span class="token keyword">SELECT</span> </span>
<span class="line">  score<span class="token punctuation">,</span></span>
<span class="line">  <span class="token keyword">CASE</span> </span>
<span class="line">    <span class="token keyword">WHEN</span> score <span class="token operator">&gt;=</span> <span class="token number">90</span> <span class="token keyword">THEN</span> <span class="token string">&#39;优秀&#39;</span></span>
<span class="line">    <span class="token keyword">WHEN</span> score <span class="token operator">&gt;=</span> <span class="token number">80</span> <span class="token keyword">THEN</span> <span class="token string">&#39;良好&#39;</span></span>
<span class="line">    <span class="token keyword">ELSE</span> <span class="token string">&#39;一般&#39;</span></span>
<span class="line">  <span class="token keyword">END</span> <span class="token keyword">AS</span> grade</span>
<span class="line"><span class="token keyword">FROM</span> students<span class="token punctuation">;</span></span>
<span class="line"></span></code></pre><div class="line-numbers" aria-hidden="true" style="counter-reset:line-number 0;"><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div></div></div></li></ul></li></ul><p><strong>使用场景</strong>：</p><ul><li>在 <code>SELECT</code> 子句中直接处理字段值（如格式化、计算）。</li><li>在 <code>WHERE</code> 子句中过滤数据（如 <code>WHERE LENGTH(name) &gt; 5</code>）。</li><li>在 <code>ORDER BY</code>、<code>GROUP BY</code> 子句中配合使用（如按字符串长度排序）。</li></ul><h4 id="二、聚合函数-aggregate-functions" tabindex="-1"><a class="header-anchor" href="#二、聚合函数-aggregate-functions"><span><strong>二、聚合函数（Aggregate Functions）</strong></span></a></h4><p><strong>定义</strong>：对一组数据进行统计计算，返回一个汇总结果（多行输入，返回一行）。<br><strong>特点</strong>：自动忽略 <code>NULL</code> 值（除 <code>COUNT(*)</code>），常与 <code>GROUP BY</code> 子句配合使用。</p><p><strong>常见聚合函数及示例</strong>：</p><table><thead><tr><th>函数</th><th>作用</th><th>示例</th></tr></thead><tbody><tr><td><code>COUNT(expr)</code></td><td>统计非 NULL 值的行数</td><td><code>SELECT COUNT(age) FROM users;</code>（忽略 <code>age</code> 为 NULL 的行）</td></tr><tr><td><code>COUNT(*)</code></td><td>统计所有行数（包括 NULL）</td><td><code>SELECT COUNT(*) FROM users;</code></td></tr><tr><td><code>SUM(expr)</code></td><td>求和</td><td><code>SELECT SUM(price) AS total_price FROM orders;</code></td></tr><tr><td><code>AVG(expr)</code></td><td>求平均值</td><td><code>SELECT AVG(score) AS avg_score FROM students;</code></td></tr><tr><td><code>MAX(expr)</code></td><td>求最大值</td><td><code>SELECT MAX(salary) AS max_salary FROM employees;</code></td></tr><tr><td><code>MIN(expr)</code></td><td>求最小值</td><td><code>SELECT MIN(stock_quantity) AS min_stock FROM products;</code></td></tr><tr><td><code>GROUP_CONCAT(expr)</code></td><td>分组拼接字符串（MySQL）</td><td><code>SELECT department, GROUP_CONCAT(name SEPARATOR &#39;,&#39;) AS members FROM employees GROUP BY department;</code></td></tr></tbody></table><p><strong>使用限制</strong>：</p><ul><li><strong>不能直接在 <code>WHERE</code> 子句中使用聚合函数</strong>，因为 <code>WHERE</code> 执行时数据尚未分组，聚合函数需基于分组后的数据计算。 <ul><li><strong>错误示例</strong>：<code>SELECT * FROM orders WHERE SUM(price) &gt; 1000;</code>（报错）。</li></ul></li><li><strong>若需过滤聚合结果</strong>，需使用 <code>HAVING</code> 子句（<code>HAVING</code> 在分组后执行）。<div class="language-sql line-numbers-mode" data-highlighter="prismjs" data-ext="sql" data-title="sql"><pre><code><span class="line"><span class="token keyword">SELECT</span> category<span class="token punctuation">,</span> <span class="token function">SUM</span><span class="token punctuation">(</span>price<span class="token punctuation">)</span> <span class="token keyword">AS</span> total_price</span>
<span class="line"><span class="token keyword">FROM</span> products</span>
<span class="line"><span class="token keyword">GROUP</span> <span class="token keyword">BY</span> category</span>
<span class="line"><span class="token keyword">HAVING</span> total_price <span class="token operator">&gt;</span> <span class="token number">1000</span><span class="token punctuation">;</span> <span class="token comment">-- 过滤分组后的聚合结果</span></span>
<span class="line"></span></code></pre><div class="line-numbers" aria-hidden="true" style="counter-reset:line-number 0;"><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div></div></div></li></ul><p><strong>使用场景</strong>：</p><ul><li>统计总数、平均值、总和等业务指标（如“各部门员工平均工资”）。</li><li>与 <code>GROUP BY</code> 配合，按字段分组统计（如“按月份统计订单量”）。</li><li>与 <code>DISTINCT</code> 配合（如 <code>COUNT(DISTINCT user_id)</code> 统计唯一用户数）。</li></ul><h4 id="三、其他函数分类" tabindex="-1"><a class="header-anchor" href="#三、其他函数分类"><span><strong>三、其他函数分类</strong></span></a></h4><h5 id="_1-窗口函数-window-functions" tabindex="-1"><a class="header-anchor" href="#_1-窗口函数-window-functions"><span><strong>1. 窗口函数（Window Functions）</strong></span></a></h5><ul><li><strong>作用</strong>：对分组数据进行排名、累计计算等，不改变行数（保留原始行数据）。 <ul><li>示例：<code>RANK()</code>、<code>DENSE_RANK()</code>、<code>SUM(expr) OVER (PARTITION BY col ORDER BY col)</code>。</li></ul><div class="language-sql line-numbers-mode" data-highlighter="prismjs" data-ext="sql" data-title="sql"><pre><code><span class="line"><span class="token keyword">SELECT</span> </span>
<span class="line">  employee_name<span class="token punctuation">,</span> </span>
<span class="line">  salary<span class="token punctuation">,</span> </span>
<span class="line">  RANK<span class="token punctuation">(</span><span class="token punctuation">)</span> <span class="token keyword">OVER</span> <span class="token punctuation">(</span><span class="token keyword">ORDER</span> <span class="token keyword">BY</span> salary <span class="token keyword">DESC</span><span class="token punctuation">)</span> <span class="token keyword">AS</span> salary_rank</span>
<span class="line"><span class="token keyword">FROM</span> employees<span class="token punctuation">;</span></span>
<span class="line"></span></code></pre><div class="line-numbers" aria-hidden="true" style="counter-reset:line-number 0;"><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div></div></div></li><li><strong>使用场景</strong>：报表中的排名、同组内对比（如“各部门员工薪资排名”）。</li></ul><h5 id="_2-系统函数-system-functions" tabindex="-1"><a class="header-anchor" href="#_2-系统函数-system-functions"><span><strong>2. 系统函数（System Functions）</strong></span></a></h5><ul><li><strong>作用</strong>：获取数据库系统信息（用户、版本、当前时间等）。 <ul><li>示例：<code>USER()</code>（获取当前用户，MySQL）、<code>VERSION()</code>（获取数据库版本）。</li></ul></li></ul>`,28)]))}const d=n(o,[["render",l]]),p=JSON.parse('{"path":"/blogs/shujuku/SQL hanshufenleijishiyongchangjing.html","title":"SQL 函数分类及使用场景","lang":"en-US","frontmatter":{"title":"SQL 函数分类及使用场景","date":"2025-05-21T00:00:00.000Z","tags":["数据库","MYSQL"],"categories":["数据库"]},"headers":[{"level":3,"title":"SQL 函数分类及使用场景","slug":"sql-函数分类及使用场景","link":"#sql-函数分类及使用场景","children":[]}],"git":{},"filePathRelative":"blogs/数据库/SQL 函数分类及使用场景.md"}');export{d as comp,p as data};
