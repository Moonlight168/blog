import{_ as i,c as a,a as n,o as t}from"./app-BwNUJfUp.js";const s={};function l(d,e){return t(),a("div",null,e[0]||(e[0]=[n(`<p>以下是整理优化后的博客内容，保留了原文的详细性，但使结构更清晰、语言更简洁，并补充了一些小细节：</p><hr><h1 id="使用-git-拉取远程仓库并与本地对比合并" tabindex="-1"><a class="header-anchor" href="#使用-git-拉取远程仓库并与本地对比合并"><span>使用 Git 拉取远程仓库并与本地对比合并</span></a></h1><h2 id="一、前言" tabindex="-1"><a class="header-anchor" href="#一、前言"><span>一、前言</span></a></h2><p>在团队协作或本地代码落后于远程仓库时，我们需要将远程仓库的变更同步到本地，并与当前代码进行对比和合并，避免丢失本地修改。本文将通过命令行方式，说明从查看远程仓库到合并的完整流程。</p><h2 id="二、查看远程仓库信息" tabindex="-1"><a class="header-anchor" href="#二、查看远程仓库信息"><span>二、查看远程仓库信息</span></a></h2><p>使用以下命令查看远程仓库的别名（默认一般为 <code>origin</code>）及对应 URL：</p><div class="language-bash line-numbers-mode" data-highlighter="prismjs" data-ext="sh" data-title="sh"><pre><code><span class="line"><span class="token function">git</span> remote <span class="token parameter variable">-v</span></span>
<span class="line"></span></code></pre><div class="line-numbers" aria-hidden="true" style="counter-reset:line-number 0;"><div class="line-number"></div></div></div><p>输出示例：</p><div class="language-text line-numbers-mode" data-highlighter="prismjs" data-ext="text" data-title="text"><pre><code><span class="line">origin  https://github.com/user/project.git (fetch)</span>
<span class="line">origin  https://github.com/user/project.git (push)</span>
<span class="line"></span></code></pre><div class="line-numbers" aria-hidden="true" style="counter-reset:line-number 0;"><div class="line-number"></div><div class="line-number"></div></div></div><h2 id="三、拉取远程更新-不合并" tabindex="-1"><a class="header-anchor" href="#三、拉取远程更新-不合并"><span>三、拉取远程更新（不合并）</span></a></h2><p>使用 <code>git fetch</code> 获取远程分支的最新提交，<strong>不会自动合并到当前分支</strong>：</p><div class="language-bash line-numbers-mode" data-highlighter="prismjs" data-ext="sh" data-title="sh"><pre><code><span class="line"><span class="token function">git</span> fetch origin</span>
<span class="line"></span></code></pre><div class="line-numbers" aria-hidden="true" style="counter-reset:line-number 0;"><div class="line-number"></div></div></div><p>说明：</p><ul><li><code>origin</code> 是远程仓库名。</li><li>该命令只更新远程跟踪分支，如 <code>origin/master</code>，不修改本地代码。</li></ul><h2 id="四、对比远程与本地文件差异" tabindex="-1"><a class="header-anchor" href="#四、对比远程与本地文件差异"><span>四、对比远程与本地文件差异</span></a></h2><p>若需对比本地文件与远程分支的差异：</p><div class="language-bash line-numbers-mode" data-highlighter="prismjs" data-ext="sh" data-title="sh"><pre><code><span class="line"><span class="token function">git</span> <span class="token function">diff</span> origin/master -- path/to/file</span>
<span class="line"></span></code></pre><div class="line-numbers" aria-hidden="true" style="counter-reset:line-number 0;"><div class="line-number"></div></div></div><p>说明：</p><ul><li><code>origin/master</code> 表示远程主分支。</li><li><code>--</code> 后为目标文件路径。</li></ul><blockquote><p>若不确定远程分支名，可使用 <code>git branch -r</code> 查看所有远程分支。</p></blockquote><h2 id="五、用远程文件覆盖本地文件-可选" tabindex="-1"><a class="header-anchor" href="#五、用远程文件覆盖本地文件-可选"><span>五、用远程文件覆盖本地文件（可选）</span></a></h2><p>若希望用远程文件内容替换本地某个文件：</p><div class="language-bash line-numbers-mode" data-highlighter="prismjs" data-ext="sh" data-title="sh"><pre><code><span class="line"><span class="token function">git</span> checkout origin/master -- path/to/file</span>
<span class="line"></span></code></pre><div class="line-numbers" aria-hidden="true" style="counter-reset:line-number 0;"><div class="line-number"></div></div></div><p>说明：</p><ul><li>不会自动合并，只是拷贝远程文件到本地工作区。</li><li>可用于对比、还原、修复某些特定文件。</li></ul><h2 id="六、合并远程分支到本地分支" tabindex="-1"><a class="header-anchor" href="#六、合并远程分支到本地分支"><span>六、合并远程分支到本地分支</span></a></h2><p>确认无误后，将远程分支合并到当前分支：</p><div class="language-bash line-numbers-mode" data-highlighter="prismjs" data-ext="sh" data-title="sh"><pre><code><span class="line"><span class="token function">git</span> merge origin/master</span>
<span class="line"></span></code></pre><div class="line-numbers" aria-hidden="true" style="counter-reset:line-number 0;"><div class="line-number"></div></div></div><p>说明：</p><ul><li>建议先 <code>fetch</code>，再 <code>merge</code>，可以控制流程。</li><li><code>git pull</code> = <code>git fetch</code> + <code>git merge</code>，适合快速同步。</li></ul><h2 id="七、解决合并冲突" tabindex="-1"><a class="header-anchor" href="#七、解决合并冲突"><span>七、解决合并冲突</span></a></h2><p>若本地和远程内容冲突，Git 会提示：</p><div class="language-text line-numbers-mode" data-highlighter="prismjs" data-ext="text" data-title="text"><pre><code><span class="line">CONFLICT (content): Merge conflict in path/to/file</span>
<span class="line"></span></code></pre><div class="line-numbers" aria-hidden="true" style="counter-reset:line-number 0;"><div class="line-number"></div></div></div><p>冲突格式如下：</p><div class="language-text line-numbers-mode" data-highlighter="prismjs" data-ext="text" data-title="text"><pre><code><span class="line">&lt;&lt;&lt;&lt;&lt;&lt;&lt; HEAD</span>
<span class="line">本地修改</span>
<span class="line">=======</span>
<span class="line">远程修改</span>
<span class="line">&gt;&gt;&gt;&gt;&gt;&gt;&gt; origin/master</span>
<span class="line"></span></code></pre><div class="line-numbers" aria-hidden="true" style="counter-reset:line-number 0;"><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div></div></div><p><strong>解决步骤：</strong></p><ol><li>手动修改冲突内容，删除冲突标记。</li><li>使用 <code>git add 文件名</code> 标记冲突解决。</li><li>执行 <code>git commit</code> 提交合并。</li></ol><h2 id="八、常用命令速查表" tabindex="-1"><a class="header-anchor" href="#八、常用命令速查表"><span>八、常用命令速查表</span></a></h2><table><thead><tr><th>场景</th><th>命令</th></tr></thead><tbody><tr><td>查看远程仓库信息</td><td><code>git remote -v</code></td></tr><tr><td>拉取远程更新（不合并）</td><td><code>git fetch origin</code></td></tr><tr><td>对比文件差异</td><td><code>git diff origin/master -- path/to/file</code></td></tr><tr><td>获取远程文件替换本地</td><td><code>git checkout origin/master -- path/to/file</code></td></tr><tr><td>合并远程分支到当前分支</td><td><code>git merge origin/master</code></td></tr><tr><td>解决冲突后提交</td><td><code>git add . &amp;&amp; git commit -m &quot;fix conflict&quot;</code></td></tr></tbody></table>`,40)]))}const c=i(s,[["render",l]]),o=JSON.parse('{"path":"/blogs/kaifagongju/ruheshiyonggitlaquyuanchengcangkuzuixinwenjianyubendicangkudewenjianduibihebing.html","title":"使用 Git 拉取远程仓库并与本地文件对比合并","lang":"en-US","frontmatter":{"title":"使用 Git 拉取远程仓库并与本地文件对比合并","date":"2025-06-09T00:00:00.000Z","categories":["开发工具"],"tags":["Git","开发工具"]},"headers":[{"level":2,"title":"一、前言","slug":"一、前言","link":"#一、前言","children":[]},{"level":2,"title":"二、查看远程仓库信息","slug":"二、查看远程仓库信息","link":"#二、查看远程仓库信息","children":[]},{"level":2,"title":"三、拉取远程更新（不合并）","slug":"三、拉取远程更新-不合并","link":"#三、拉取远程更新-不合并","children":[]},{"level":2,"title":"四、对比远程与本地文件差异","slug":"四、对比远程与本地文件差异","link":"#四、对比远程与本地文件差异","children":[]},{"level":2,"title":"五、用远程文件覆盖本地文件（可选）","slug":"五、用远程文件覆盖本地文件-可选","link":"#五、用远程文件覆盖本地文件-可选","children":[]},{"level":2,"title":"六、合并远程分支到本地分支","slug":"六、合并远程分支到本地分支","link":"#六、合并远程分支到本地分支","children":[]},{"level":2,"title":"七、解决合并冲突","slug":"七、解决合并冲突","link":"#七、解决合并冲突","children":[]},{"level":2,"title":"八、常用命令速查表","slug":"八、常用命令速查表","link":"#八、常用命令速查表","children":[]}],"git":{},"filePathRelative":"blogs/开发工具/如何使用git拉取远程仓库最新文件与本地仓库的文件对比合并.md"}');export{c as comp,o as data};
