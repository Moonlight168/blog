import{_ as d,c as e,a as o,o as c}from"./app-BwNUJfUp.js";const a={};function r(h,t){return c(),e("div",null,t[0]||(t[0]=[o('<p>在日常开发中，MySQL 内置了大量函数，掌握常见函数的分类及使用场景，有助于提升 SQL 编写效率与可读性。</p><h2 id="一、字符串函数" tabindex="-1"><a class="header-anchor" href="#一、字符串函数"><span>一、字符串函数</span></a></h2><p>用于处理文本内容，常见于数据清洗、字符串拼接、截取等场景。</p><table><thead><tr><th>函数</th><th>说明</th><th>示例</th></tr></thead><tbody><tr><td><code>CONCAT(a, b)</code></td><td>字符串拼接</td><td><code>CONCAT(&#39;A&#39;, &#39;B&#39;) → &#39;AB&#39;</code></td></tr><tr><td><code>SUBSTRING(s, start, len)</code></td><td>截取子串</td><td><code>SUBSTRING(&#39;hello&#39;, 2, 3) → &#39;ell&#39;</code></td></tr><tr><td><code>REPLACE(s, a, b)</code></td><td>替换子串</td><td><code>REPLACE(&#39;a-b-c&#39;, &#39;-&#39;, &#39;_&#39;) → &#39;a_b_c&#39;</code></td></tr><tr><td><code>LENGTH(s)</code></td><td>字节长度（与字符集相关）</td><td><code>LENGTH(&#39;中&#39;) → 3（UTF-8下）</code></td></tr><tr><td><code>TRIM(s)</code></td><td>去除首尾空格</td><td><code>TRIM(&#39; abc &#39;) → &#39;abc&#39;</code></td></tr></tbody></table><h2 id="二、数值函数" tabindex="-1"><a class="header-anchor" href="#二、数值函数"><span>二、数值函数</span></a></h2><p>处理数值计算，常用于统计分析、字段计算等。</p><table><thead><tr><th>函数</th><th>说明</th><th>示例</th></tr></thead><tbody><tr><td><code>ABS(n)</code></td><td>绝对值</td><td><code>ABS(-5) → 5</code></td></tr><tr><td><code>CEIL(n)</code></td><td>向上取整</td><td><code>CEIL(1.3) → 2</code></td></tr><tr><td><code>FLOOR(n)</code></td><td>向下取整</td><td><code>FLOOR(1.8) → 1</code></td></tr><tr><td><code>ROUND(n, d)</code></td><td>四舍五入到指定位数</td><td><code>ROUND(1.456, 2) → 1.46</code></td></tr><tr><td><code>MOD(a, b)</code></td><td>取模（a除以b的余数）</td><td><code>MOD(10, 3) → 1</code></td></tr><tr><td><code>RAND()</code></td><td>生成0~1之间的随机小数</td><td><code>RAND() → 0.3749...</code></td></tr></tbody></table><h2 id="三、日期时间函数" tabindex="-1"><a class="header-anchor" href="#三、日期时间函数"><span>三、日期时间函数</span></a></h2><p>用于处理时间计算、格式化、提取日期字段等。</p><table><thead><tr><th>函数</th><th>说明</th><th>示例</th></tr></thead><tbody><tr><td><code>NOW()</code></td><td>当前时间（含日期）</td><td><code>NOW() → &#39;2025-06-04 15:30:00&#39;</code></td></tr><tr><td><code>CURDATE()</code></td><td>当前日期</td><td><code>CURDATE() → &#39;2025-06-04&#39;</code></td></tr><tr><td><code>DATE_FORMAT(d, fmt)</code></td><td>格式化日期</td><td><code>DATE_FORMAT(NOW(), &#39;%Y-%m&#39;) → &#39;2025-06&#39;</code></td></tr><tr><td><code>DATEDIFF(a, b)</code></td><td>相差天数（a-b）</td><td><code>DATEDIFF(&#39;2025-06-10&#39;, &#39;2025-06-01&#39;) → 9</code></td></tr><tr><td><code>YEAR(d)</code> / <code>MONTH(d)</code> / <code>DAY(d)</code></td><td>提取年、月、日字段</td><td><code>YEAR(&#39;2024-12-01&#39;) → 2024</code></td></tr><tr><td><code>TIMESTAMPDIFF(unit, a, b)</code></td><td>时间差，按指定单位（年/月/日/时等）</td><td><code>TIMESTAMPDIFF(MINUTE, a, b)</code></td></tr><tr><td><code>DATE_ADD(date, INTERVAL n unit)</code></td><td>指定日期加上时间间隔</td><td><code>DATE_ADD(&#39;2025-06-01&#39;, INTERVAL 7 DAY) → &#39;2025-06-08&#39;</code></td></tr><tr><td><code>DATE_SUB(date, INTERVAL n unit)</code></td><td>指定日期减去时间间隔</td><td><code>DATE_SUB(&#39;2025-06-01&#39;, INTERVAL 3 DAY) → &#39;2025-05-29&#39;</code></td></tr></tbody></table><h2 id="四、聚合函数" tabindex="-1"><a class="header-anchor" href="#四、聚合函数"><span>四、聚合函数</span></a></h2><p>常与 <code>GROUP BY</code> 一起使用，执行分组统计操作。</p><table><thead><tr><th>函数</th><th>说明</th><th>示例</th></tr></thead><tbody><tr><td><code>COUNT(*)</code></td><td>统计记录数</td><td><code>COUNT(*) → 总行数</code></td></tr><tr><td><code>SUM(expr)</code></td><td>求和</td><td><code>SUM(salary)</code></td></tr><tr><td><code>AVG(expr)</code></td><td>平均值</td><td><code>AVG(score)</code></td></tr><tr><td><code>MAX(expr)</code></td><td>最大值</td><td><code>MAX(age)</code></td></tr><tr><td><code>MIN(expr)</code></td><td>最小值</td><td><code>MIN(created_at)</code></td></tr><tr><td><code>GROUP_CONCAT()</code></td><td>将同组多个值拼接成字符串</td><td><code>GROUP_CONCAT(name SEPARATOR &#39;,&#39;) → &#39;A,B,C&#39;</code></td></tr></tbody></table><h2 id="五、控制流函数" tabindex="-1"><a class="header-anchor" href="#五、控制流函数"><span>五、控制流函数</span></a></h2><p>用于在 SQL 中进行条件判断、实现逻辑控制。</p><table><thead><tr><th>函数</th><th>说明</th><th>示例</th></tr></thead><tbody><tr><td><code>IF(expr, a, b)</code></td><td>类似三元表达式，条件判断</td><td><code>IF(score &gt; 60, &#39;及格&#39;, &#39;不及格&#39;)</code></td></tr><tr><td><code>IFNULL(expr, default)</code></td><td>若为空则返回默认值</td><td><code>IFNULL(nick_name, &#39;匿名&#39;)</code></td></tr><tr><td><code>CASE WHEN ... THEN ... END</code></td><td>多条件判断，相当于 switch-case</td><td><code>CASE gender WHEN &#39;M&#39; THEN &#39;男&#39; ... END</code></td></tr><tr><td><code>NULLIF(a, b)</code></td><td>若a=b则返回NULL，否则返回a</td><td><code>NULLIF(5, 5) → NULL</code></td></tr></tbody></table><h2 id="六、json-函数-mysql-5-7" tabindex="-1"><a class="header-anchor" href="#六、json-函数-mysql-5-7"><span>六、JSON 函数（MySQL 5.7+）</span></a></h2><p>适用于存储和查询 JSON 格式的数据。</p><table><thead><tr><th>函数</th><th>说明</th><th>示例</th></tr></thead><tbody><tr><td><code>JSON_OBJECT(k, v, ...)</code></td><td>构造 JSON 对象</td><td><code>JSON_OBJECT(&#39;id&#39;, 1, &#39;name&#39;, &#39;Tom&#39;)</code></td></tr><tr><td><code>JSON_EXTRACT(json, path)</code></td><td>提取 JSON 字段（别名：<code>-&gt;</code>）</td><td><code>JSON_EXTRACT(data, &#39;$.name&#39;)</code></td></tr><tr><td><code>JSON_SET(json, path, val)</code></td><td>更新 JSON 中的指定字段</td><td><code>JSON_SET(data, &#39;$.age&#39;, 18)</code></td></tr><tr><td><code>JSON_ARRAY(v1, v2, ...)</code></td><td>构造 JSON 数组</td><td><code>JSON_ARRAY(&#39;A&#39;, &#39;B&#39;)</code></td></tr></tbody></table><h2 id="七、窗口函数-需配合-over-使用" tabindex="-1"><a class="header-anchor" href="#七、窗口函数-需配合-over-使用"><span>七、窗口函数（需配合 <code>OVER</code> 使用）</span></a></h2><p>不影响原始行数，常用于排名、累计值等分析场景。</p><table><thead><tr><th>函数</th><th>说明</th><th>示例</th></tr></thead><tbody><tr><td><code>ROW_NUMBER()</code></td><td>分组内行号</td><td><code>ROW_NUMBER() OVER (PARTITION BY dept ORDER BY salary)</code></td></tr><tr><td><code>RANK()</code> / <code>DENSE_RANK()</code></td><td>分组排名，遇并列名次会跳过或不跳</td><td><code>RANK() OVER (PARTITION BY dept ORDER BY salary DESC)</code></td></tr><tr><td><code>SUM()</code> / <code>AVG()</code> / <code>COUNT()</code> 等</td><td>可作为窗口函数使用</td><td><code>SUM(sales) OVER (PARTITION BY region ORDER BY date)</code></td></tr></tbody></table>',22)]))}const n=d(a,[["render",r]]),s=JSON.parse('{"path":"/blogs/shujuku/SQL hanshufenleijishiyongchangjing.html","title":"MySQL 函数分类及使用场景","lang":"en-US","frontmatter":{"title":"MySQL 函数分类及使用场景","date":"2025-05-21T00:00:00.000Z","tags":["数据库","MYSQL"],"categories":["数据库"]},"headers":[{"level":2,"title":"一、字符串函数","slug":"一、字符串函数","link":"#一、字符串函数","children":[]},{"level":2,"title":"二、数值函数","slug":"二、数值函数","link":"#二、数值函数","children":[]},{"level":2,"title":"三、日期时间函数","slug":"三、日期时间函数","link":"#三、日期时间函数","children":[]},{"level":2,"title":"四、聚合函数","slug":"四、聚合函数","link":"#四、聚合函数","children":[]},{"level":2,"title":"五、控制流函数","slug":"五、控制流函数","link":"#五、控制流函数","children":[]},{"level":2,"title":"六、JSON 函数（MySQL 5.7+）","slug":"六、json-函数-mysql-5-7","link":"#六、json-函数-mysql-5-7","children":[]},{"level":2,"title":"七、窗口函数（需配合 OVER 使用）","slug":"七、窗口函数-需配合-over-使用","link":"#七、窗口函数-需配合-over-使用","children":[]}],"git":{},"filePathRelative":"blogs/数据库/SQL 函数分类及使用场景.md"}');export{n as comp,s as data};
