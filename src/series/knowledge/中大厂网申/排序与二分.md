---
title: 排序与二分
order: 8
---

> 排序和二分是**最基本但最常考**的算法。本篇覆盖：10 大排序算法对比、二分查找各种变体、二分答案套路、TopK 问题。

## 一、排序算法总览

### 1. 复杂度对比

| 排序 | 时间（平均）| 时间（最坏）| 空间 | 稳定性 | 适用场景 |
|------|-------------|--------------|------|--------|----------|
| 冒泡 | O(n²) | O(n²) | O(1) | 稳定 | 教学、小数据 |
| 选择 | O(n²) | O(n²) | O(1) | 不稳定 | 教学 |
| 插入 | O(n²) | O(n²) | O(1) | 稳定 | 基本有序数据 |
| 希尔 | O(n log n) ~ O(n²) | O(n²) | O(1) | 不稳定 | 中等规模 |
| **归并** | **O(n log n)** | O(n log n) | O(n) | 稳定 | 链表、大数据、要求稳定 |
| **快速** | **O(n log n)** | O(n²) | O(log n) | 不稳定 | 通用、内部排序 |
| 堆 | O(n log n) | O(n log n) | O(1) | 不稳定 | TopK、优先级队列 |
| 计数 | O(n + k) | O(n + k) | O(k) | 稳定 | 范围小、整数 |
| 桶 | O(n + k) | O(n²) | O(n + k) | 稳定 | 均匀分布 |
| 基数 | O(n × d) | O(n × d) | O(n + k) | 稳定 | 大数、字符串 |

### 2. Java 内置排序

```java
// 1. 基本类型（快排优化）
int[] arr1 = {3, 1, 4, 1, 5, 9, 2, 6};
Arrays.sort(arr1);  // 升序

// 2. 对象数组（归并）
Integer[] arr2 = {3, 1, 4, 1, 5};
Arrays.sort(arr2, (a, b) -> b - a);  // 降序

// 3. List 排序
List<Integer> list = new ArrayList<>(Arrays.asList(3, 1, 4));
Collections.sort(list);  // 升序
list.sort((a, b) -> b - a);  // 降序

// 4. 部分排序
Arrays.sort(arr, 0, 3);  // 排序 [0, 3) 区间
```

### 3. 排序稳定性

- **稳定**：相等元素的相对顺序保持不变
- **应用**：多级排序（如先按年龄、再按姓名）

---

## 二、十大排序算法实现（Java）

### 1. 冒泡排序

```java
public void bubbleSort(int[] arr) {
    int n = arr.length;
    for (int i = 0; i < n - 1; i++) {
        boolean swapped = false;
        for (int j = 0; j < n - 1 - i; j++) {
            if (arr[j] > arr[j + 1]) {
                int tmp = arr[j];
                arr[j] = arr[j + 1];
                arr[j + 1] = tmp;
                swapped = true;
            }
        }
        if (!swapped) break;  // 已排序，提前退出
    }
}
```

### 2. 选择排序

```java
public void selectionSort(int[] arr) {
    int n = arr.length;
    for (int i = 0; i < n - 1; i++) {
        int minIdx = i;
        for (int j = i + 1; j < n; j++) {
            if (arr[j] < arr[minIdx]) minIdx = j;
        }
        int tmp = arr[i];
        arr[i] = arr[minIdx];
        arr[minIdx] = tmp;
    }
}
```

### 3. 插入排序

```java
public void insertionSort(int[] arr) {
    int n = arr.length;
    for (int i = 1; i < n; i++) {
        int key = arr[i];
        int j = i - 1;
        while (j >= 0 && arr[j] > key) {
            arr[j + 1] = arr[j];
            j--;
        }
        arr[j + 1] = key;
    }
}
```

### 4. 希尔排序

```java
public void shellSort(int[] arr) {
    int n = arr.length;
    for (int gap = n / 2; gap > 0; gap /= 2) {
        for (int i = gap; i < n; i++) {
            int tmp = arr[i];
            int j = i;
            while (j >= gap && arr[j - gap] > tmp) {
                arr[j] = arr[j - gap];
                j -= gap;
            }
            arr[j] = tmp;
        }
    }
}
```

### 5. 归并排序

```java
public void mergeSort(int[] arr, int left, int right) {
    if (left >= right) return;
    int mid = left + (right - left) / 2;
    mergeSort(arr, left, mid);
    mergeSort(arr, mid + 1, right);
    merge(arr, left, mid, right);
}

private void merge(int[] arr, int left, int mid, int right) {
    int[] tmp = new int[right - left + 1];
    int i = left, j = mid + 1, k = 0;
    while (i <= mid && j <= right) {
        if (arr[i] <= arr[j]) tmp[k++] = arr[i++];
        else tmp[k++] = arr[j++];
    }
    while (i <= mid) tmp[k++] = arr[i++];
    while (j <= right) tmp[k++] = arr[j++];
    System.arraycopy(tmp, 0, arr, left, tmp.length);
}
```

### 6. 快速排序

```java
public void quickSort(int[] arr, int left, int right) {
    if (left >= right) return;
    int pivot = partition(arr, left, right);
    quickSort(arr, left, pivot - 1);
    quickSort(arr, pivot + 1, right);
}

private int partition(int[] arr, int left, int right) {
    int pivot = arr[right];
    int i = left - 1;
    for (int j = left; j < right; j++) {
        if (arr[j] < pivot) {
            i++;
            int tmp = arr[i];
            arr[i] = arr[j];
            arr[j] = tmp;
        }
    }
    int tmp = arr[i + 1];
    arr[i + 1] = arr[right];
    arr[right] = tmp;
    return i + 1;
}
```

### 7. 堆排序

```java
public void heapSort(int[] arr) {
    int n = arr.length;
    // 建堆
    for (int i = n / 2 - 1; i >= 0; i--) {
        heapify(arr, n, i);
    }
    // 排序
    for (int i = n - 1; i > 0; i--) {
        int tmp = arr[0];
        arr[0] = arr[i];
        arr[i] = tmp;
        heapify(arr, i, 0);
    }
}

private void heapify(int[] arr, int n, int i) {
    int largest = i;
    int left = 2 * i + 1;
    int right = 2 * i + 2;
    if (left < n && arr[left] > arr[largest]) largest = left;
    if (right < n && arr[right] > arr[largest]) largest = right;
    if (largest != i) {
        int tmp = arr[i];
        arr[i] = arr[largest];
        arr[largest] = tmp;
        heapify(arr, n, largest);
    }
}
```

### 8. 计数排序

```java
public void countingSort(int[] arr) {
    int max = Arrays.stream(arr).max().getAsInt();
    int min = Arrays.stream(arr).min().getAsInt();
    int range = max - min + 1;
    int[] count = new int[range];
    for (int num : arr) count[num - min]++;
    int idx = 0;
    for (int i = 0; i < range; i++) {
        while (count[i]-- > 0) arr[idx++] = i + min;
    }
}
```

### 9. 桶排序

```java
public void bucketSort(float[] arr) {
    int n = arr.length;
    if (n <= 0) return;
    
    @SuppressWarnings("unchecked")
    List<Float>[] buckets = new ArrayList[n];
    for (int i = 0; i < n; i++) buckets[i] = new ArrayList<>();
    
    for (float num : arr) {
        int idx = (int) (num * n);
        buckets[idx].add(num);
    }
    
    for (List<Float> bucket : buckets) Collections.sort(bucket);
    
    int idx = 0;
    for (List<Float> bucket : buckets) {
        for (float num : bucket) arr[idx++] = num;
    }
}
```

### 10. 基数排序

```java
public void radixSort(int[] arr) {
    int max = Arrays.stream(arr).max().getAsInt();
    for (int exp = 1; max / exp > 0; exp *= 10) {
        countingSortByDigit(arr, exp);
    }
}

private void countingSortByDigit(int[] arr, int exp) {
    int n = arr.length;
    int[] output = new int[n];
    int[] count = new int[10];
    
    for (int num : arr) count[(num / exp) % 10]++;
    for (int i = 1; i < 10; i++) count[i] += count[i - 1];
    for (int i = n - 1; i >= 0; i--) {
        output[--count[(arr[i] / exp) % 10]] = arr[i];
    }
    System.arraycopy(output, 0, arr, 0, n);
}
```

---

## 三、二分查找

### 1. 标准二分

```java
public int binarySearch(int[] nums, int target) {
    int left = 0, right = nums.length - 1;
    while (left <= right) {
        int mid = left + (right - left) / 2;  // 防溢出
        if (nums[mid] == target) return mid;
        else if (nums[mid] < target) left = mid + 1;
        else right = mid - 1;
    }
    return -1;
}
```

### 2. Java 内置二分

```java
int idx = Arrays.binarySearch(arr, target);
// 返回 target 的下标，不存在返回 -(插入点 + 1)
```

### 3. 变体 1：查找第一个等于 target

```java
public int firstEqual(int[] nums, int target) {
    int left = 0, right = nums.length - 1;
    while (left <= right) {
        int mid = left + (right - left) / 2;
        if (nums[mid] < target) left = mid + 1;
        else right = mid - 1;  // 关键：相等时不返回，继续向左
    }
    return left < nums.length && nums[left] == target ? left : -1;
}
```

### 4. 变体 2：查找最后一个等于 target

```java
public int lastEqual(int[] nums, int target) {
    int left = 0, right = nums.length - 1;
    while (left <= right) {
        int mid = left + (right - left) / 2;
        if (nums[mid] > target) right = mid - 1;
        else left = mid + 1;  // 关键：相等时不返回，继续向右
    }
    return right >= 0 && nums[right] == target ? right : -1;
}
```

### 5. 变体 3：搜索插入位置

```java
public int searchInsert(int[] nums, int target) {
    int left = 0, right = nums.length - 1;
    while (left <= right) {
        int mid = left + (right - left) / 2;
        if (nums[mid] < target) left = mid + 1;
        else right = mid - 1;
    }
    return left;  // 插入位置
}
```

### 6. 变体 4：寻找峰值

```java
public int findPeakElement(int[] nums) {
    int left = 0, right = nums.length - 1;
    while (left < right) {
        int mid = left + (right - left) / 2;
        if (nums[mid] < nums[mid + 1]) left = mid + 1;
        else right = mid;
    }
    return left;
}
```

### 7. 旋转数组中的搜索

```java
public int search(int[] nums, int target) {
    int left = 0, right = nums.length - 1;
    while (left <= right) {
        int mid = left + (right - left) / 2;
        if (nums[mid] == target) return mid;
        if (nums[left] <= nums[mid]) {  // 左侧有序
            if (target >= nums[left] && target < nums[mid]) right = mid - 1;
            else left = mid + 1;
        } else {  // 右侧有序
            if (target > nums[mid] && target <= nums[right]) left = mid + 1;
            else right = mid - 1;
        }
    }
    return -1;
}
```

---

## 四、二分答案（高级）

**核心思想**：当**答案具有单调性**时，可以通过二分"答案"来求解。

### 1. 经典套路

```java
// 求满足条件的最小值
int left = minPossible, right = maxPossible;
while (left < right) {
    int mid = left + (right - left) / 2;
    if (canAchieve(mid)) right = mid;  // 可以达到，尝试更小
    else left = mid + 1;                // 不行，增大
}
return left;

// 求满足条件的最大值
int left = minPossible, right = maxPossible;
while (left < right) {
    int mid = left + (right - left + 1) / 2;  // 向上取整防死循环
    if (canAchieve(mid)) left = mid;          // 可以达到，尝试更大
    else right = mid - 1;
}
return left;
```

### 2. 爱吃香蕉的珂珂（LeetCode 875）

**题意**：N 堆香蕉，H 小时内吃完，最小速度？

**核心**：速度越大，时间越短（单调性）

```java
public int minEatingSpeed(int[] piles, int h) {
    int left = 1, right = Arrays.stream(piles).max().getAsInt();
    while (left < right) {
        int mid = left + (right - left) / 2;
        if (canEat(piles, mid, h)) right = mid;
        else left = mid + 1;
    }
    return left;
}

private boolean canEat(int[] piles, int speed, int h) {
    int hours = 0;
    for (int p : piles) {
        hours += (p + speed - 1) / speed;
    }
    return hours <= h;
}
```

### 3. 分割数组的最大值（LeetCode 410）

```java
public int splitArray(int[] nums, int m) {
    int left = Arrays.stream(nums).max().getAsInt();
    int right = Arrays.stream(nums).sum();
    while (left < right) {
        int mid = left + (right - left) / 2;
        if (canSplit(nums, mid, m)) right = mid;
        else left = mid + 1;
    }
    return left;
}

private boolean canSplit(int[] nums, int maxSum, int m) {
    int subarrays = 1, curSum = 0;
    for (int num : nums) {
        if (curSum + num > maxSum) {
            subarrays++;
            curSum = num;
        } else {
            curSum += num;
        }
    }
    return subarrays <= m;
}
```

### 4. 制作 m 束花所需的最少天数（LeetCode 1482）

```java
public int minDays(int[] bloomDay, int m, int k) {
    if ((long) m * k > bloomDay.length) return -1;
    int left = Arrays.stream(bloomDay).min().getAsInt();
    int right = Arrays.stream(bloomDay).max().getAsInt();
    while (left < right) {
        int mid = left + (right - left) / 2;
        if (canMake(bloomDay, mid, m, k)) right = mid;
        else left = mid + 1;
    }
    return left;
}

private boolean canMake(int[] bloomDay, int day, int m, int k) {
    int bouquets = 0, flowers = 0;
    for (int d : bloomDay) {
        if (d <= day) {
            flowers++;
            if (flowers == k) {
                bouquets++;
                flowers = 0;
            }
        } else {
            flowers = 0;
        }
    }
    return bouquets >= m;
}
```

### 5. 找两个有序数组的中位数（LeetCode 4）— Hard

```java
public double findMedianSortedArrays(int[] nums1, int[] nums2) {
    int m = nums1.length, n = nums2.length;
    if (m > n) return findMedianSortedArrays(nums2, nums1);
    int left = 0, right = m;
    while (left <= right) {
        int i = left + (right - left) / 2;
        int j = (m + n + 1) / 2 - i;
        int nums1Left = i == 0 ? Integer.MIN_VALUE : nums1[i - 1];
        int nums1Right = i == m ? Integer.MAX_VALUE : nums1[i];
        int nums2Left = j == 0 ? Integer.MIN_VALUE : nums2[j - 1];
        int nums2Right = j == n ? Integer.MAX_VALUE : nums2[j];
        if (nums1Left <= nums2Right && nums2Left <= nums1Right) {
            if ((m + n) % 2 == 1) return Math.max(nums1Left, nums2Left);
            return (Math.max(nums1Left, nums2Left) + Math.min(nums1Right, nums2Right)) / 2.0;
        } else if (nums1Left > nums2Right) right = i - 1;
        else left = i + 1;
    }
    return 0;
}
```

---

## 五、TopK 问题

### 1. 数组中的第 K 个最大元素（LeetCode 215）

**三种解法**：

```java
// 解法 1：堆（推荐）
public int findKthLargest(int[] nums, int k) {
    PriorityQueue<Integer> minHeap = new PriorityQueue<>();
    for (int num : nums) {
        minHeap.offer(num);
        if (minHeap.size() > k) minHeap.poll();
    }
    return minHeap.peek();
}

// 解法 2：快排 partition
public int findKthLargest(int[] nums, int k) {
    return quickSelect(nums, 0, nums.length - 1, nums.length - k);
}

private int quickSelect(int[] nums, int left, int right, int k) {
    int pivot = partition(nums, left, right);
    if (pivot == k) return nums[pivot];
    if (pivot < k) return quickSelect(nums, pivot + 1, right, k);
    return quickSelect(nums, left, pivot - 1, k);
}

private int partition(int[] nums, int left, int right) {
    int pivot = nums[right];
    int i = left;
    for (int j = left; j < right; j++) {
        if (nums[j] < pivot) {
            int tmp = nums[i];
            nums[i++] = nums[j];
            nums[j] = tmp;
        }
    }
    int tmp = nums[i];
    nums[i] = nums[right];
    nums[right] = tmp;
    return i;
}

// 解法 3：排序后取
public int findKthLargest(int[] nums, int k) {
    Arrays.sort(nums);
    return nums[nums.length - k];
}
```

### 2. 前 K 个高频元素（LeetCode 347）

```java
public int[] topKFrequent(int[] nums, int k) {
    Map<Integer, Integer> count = new HashMap<>();
    for (int num : nums) count.merge(num, 1, Integer::sum);
    
    PriorityQueue<int[]> pq = new PriorityQueue<>((a, b) -> a[1] - b[1]);
    for (Map.Entry<Integer, Integer> e : count.entrySet()) {
        pq.offer(new int[]{e.getKey(), e.getValue()});
        if (pq.size() > k) pq.poll();
    }
    return pq.stream().mapToInt(a -> a[0]).toArray();
}
```

### 3. 最接近原点的 K 个点（LeetCode 973）

```java
public int[][] kClosest(int[][] points, int k) {
    Arrays.sort(points, (a, b) -> dist(a) - dist(b));
    return Arrays.copyOfRange(points, 0, k);
}

private int dist(int[] p) {
    return p[0] * p[0] + p[1] * p[1];
}
```

### 4. 最大间距（LeetCode 164）— Hard

**核心**：桶排序

```java
public int maximumGap(int[] nums) {
    if (nums.length < 2) return 0;
    int min = Arrays.stream(nums).min().getAsInt();
    int max = Arrays.stream(nums).max().getAsInt();
    if (min == max) return 0;
    
    int n = nums.length;
    int bucketSize = Math.max(1, (max - min) / (n - 1));
    int bucketCount = (max - min) / bucketSize + 1;
    int[] bucketMin = new int[bucketCount];
    int[] bucketMax = new int[bucketCount];
    Arrays.fill(bucketMin, Integer.MAX_VALUE);
    Arrays.fill(bucketMax, Integer.MIN_VALUE);
    
    for (int num : nums) {
        int idx = (num - min) / bucketSize;
        bucketMin[idx] = Math.min(bucketMin[idx], num);
        bucketMax[idx] = Math.max(bucketMax[idx], num);
    }
    
    int res = 0, prevMax = bucketMax[0];
    for (int i = 1; i < bucketCount; i++) {
        if (bucketMin[i] == Integer.MAX_VALUE) continue;
        res = Math.max(res, bucketMin[i] - prevMax);
        prevMax = bucketMax[i];
    }
    return res;
}
```

---

## 六、自定义排序

### 1. Comparable 接口

```java
class Student implements Comparable<Student> {
    String name;
    int score;
    
    @Override
    public int compareTo(Student o) {
        return Integer.compare(this.score, o.score);  // 按分数升序
    }
}
```

### 2. Comparator 接口

```java
// Lambda 形式
Arrays.sort(students, (a, b) -> b.score - a.score);  // 按分数降序

// 复杂排序：先按分数降序，再按名字升序
Arrays.sort(students, (a, b) -> {
    if (a.score != b.score) return b.score - a.score;
    return a.name.compareTo(b.name);
});
```

### 3. 例题：根据身高重建队列（LeetCode 406）

**核心**：先按身高降序，再按 k 升序

```java
public int[][] reconstructQueue(int[][] people) {
    Arrays.sort(people, (a, b) -> {
        if (a[0] != b[0]) return b[0] - a[0];
        return a[1] - b[1];
    });
    List<int[]> res = new LinkedList<>();
    for (int[] p : people) {
        res.add(p[1], p);
    }
    return res.toArray(new int[res.size()][]);
}
```

### 4. 例题：最大数（LeetCode 179）

**核心**：自定义比较器，`(a, b) -> (b + a).compareTo(a + b)`

```java
public String largestNumber(int[] nums) {
    String[] strs = new String[nums.length];
    for (int i = 0; i < nums.length; i++) strs[i] = String.valueOf(nums[i]);
    Arrays.sort(strs, (a, b) -> (b + a).compareTo(a + b));
    if (strs[0].equals("0")) return "0";
    return String.join("", strs);
}
```

---

## 七、二分查找避坑指南

### 1. 死循环问题

```java
// 错误：left = mid 会死循环
while (left < right) {
    int mid = (left + right) / 2;
    if (...) left = mid;
    else right = mid - 1;
}

// 正确：向上取整
while (left < right) {
    int mid = left + (right - left + 1) / 2;
    if (...) left = mid;
    else right = mid - 1;
}
```

### 2. 边界条件

```java
// 错误：数组空时 left > right
while (left <= right) {
    int mid = ...
    if (...) left = mid + 1;
    else right = mid - 1;
}
return -1;  // 不存在返回 -1

// 错误：返回 left 可能越界
return left;  // 可能等于 nums.length
```

### 3. 整数溢出

```java
// 错误：left + right 可能溢出
int mid = (left + right) / 2;

// 正确：减法防溢出
int mid = left + (right - left) / 2;
```

### 4. 旋转数组判有序

```java
// 关键：通过 nums[left] <= nums[mid] 判断哪半边有序
if (nums[left] <= nums[mid]) {
    // 左半边有序
} else {
    // 右半边有序
}
```

---

## 八、20 题实战清单

### 排序（5 题）

| # | 题目 | 难度 | 核心 |
|---|------|------|------|
| 1 | 数组中的第 K 个最大元素 | Medium | 快排/堆 |
| 2 | 前 K 个高频元素 | Medium | 堆 |
| 3 | 摆动排序 II | Medium | 排序 |
| 4 | 最大数 | Medium | 自定义排序 |
| 5 | 最大间距 | Hard | 桶排序 |

### 二分（10 题）

| # | 题目 | 难度 | 核心 |
|---|------|------|------|
| 1 | 二分查找 | Easy | 二分 |
| 2 | 搜索插入位置 | Easy | 二分 |
| 3 | 搜索旋转排序数组 | Medium | 二分变形 |
| 4 | 寻找旋转排序数组的最小值 | Medium | 二分 |
| 5 | 寻找峰值 | Medium | 二分 |
| 6 | 爱吃香蕉的珂珂 | Medium | 二分答案 |
| 7 | 分割数组的最大值 | Hard | 二分答案 |
| 8 | 制作 m 束花 | Medium | 二分答案 |
| 9 | 找两个有序数组的中位数 | Hard | 二分 |
| 10 | 第一个错误版本 | Easy | 二分 |

### 自定义排序（5 题）

| # | 题目 | 难度 | 核心 |
|---|------|------|------|
| 1 | 根据身高重建队列 | Medium | 排序+插入 |
| 2 | 最大数 | Medium | 自定义比较 |
| 3 | 摆动排序 | Medium | 排序 |
| 4 | H 指数 | Medium | 排序 |
| 5 | 翻转对 | Hard | 归并 |

---

## 九、笔试实战时间分配

| 题型 | 题量 | 推荐时间 | 重点 |
|------|------|----------|------|
| 排序应用 | 1-2 题 | 5-15 分钟/题 | 调 `Arrays.sort`，写 Comparator |
| 纯二分 | 1 题 | 10-15 分钟 | 模板背熟 |
| 二分答案 | 1 题 | 20-30 分钟 | 写 `canAchieve` 函数 |
| 旋转数组 | 1 题 | 15-20 分钟 | 边界判断 |

---

## 十、常见面试追问

> Q: 排序算法的稳定性在实际中有什么用？
> A: 多级排序、数据库 ORDER BY 多次、对象排序时保持原顺序。

> Q: 为什么快速排序比归并排序快？
> A: 快排是原地排序（O(log n) 空间），缓存友好；归并需要额外 O(n) 空间。

> Q: 什么时候用堆而不是快排？
> A: 需要 TopK（堆 O(n log k) < 排序 O(n log n)），需要流式处理时。

> Q: 二分答案的核心思想是什么？
> A: 答案单调性——「答案越大越容易满足/越难满足」，就可以二分。

📌 **排序是基础，二分是利器**——掌握这 20 题，排序和二分题型覆盖 95%。
